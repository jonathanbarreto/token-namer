{
  "name": "token-namer",
  "version": "1.0.0",
  "description": "Generate consistent, structured design token names.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.7"
  }
}

